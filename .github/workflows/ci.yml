name: CI-CD

on:
    pull_request:
        branches: [main]
    
    push:
        branches: [main]

jobs:
    test:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v4

            - uses: actions/setup-python@v5
              with:
                python-version: "3.14.0"

            - run: pip install -r requirements.txt
            - run: pip install pytest ruff

            - run: ruff check .
            - run: pytest
    
    deploy_prod:
        needs: test
        if: github.ref == 'refs/heads/main'

        runs-on: ubuntu-latest

        steps:
          - name: Trigger Render Deploy
            run: curl "$RENDER_PREPROD"
            env:
                RENDER_PREPROD: ${{ secrets.RENDER_PROD }}
    deploy_preprod:
        needs: test
        if: github.ref == 'refs/heads/develop'

        runs-on: ubuntu-latest

        steps:
          - name: Trigger Render Deploy
            run: curl "$RENDER_PREPROD"
            env:
                RENDER_PREPROD: ${{ secrets.RENDER_PREPROD }}